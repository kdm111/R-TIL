#include <stdio.h>
#include <string.h>

void main(void) {
    /*char str1[10] = "cat";
    char str2[10];

    strcpy(str1, "tiger");
    strcpy(str2, str1);
    printf("str1 : %s\n", str1);
    printf("str2 : %s\n", str2);
    */

/*    char str[80];
    printf("input string : ");
    // gets(str); // 문자열의 크기를 무시하고 오버플로우를 발생시킴
    fgets(str, sizeof(str), stdin); // 문자열이 시작할 때부터
    //scanf("%s", str); // \0에서 끊어짐
    printf("      string : "); puts(str);
*/

/*    char str[80];

    str[0] = '1';str[1] = '1';str[2] = '1';
    printf("%s", str);
    */

    
}

