#include <stdio.h>
#include <stdlib.h>
#include <time.h>

void main(void) {

    /*
    // rand로 함수 뽑아내기
    int num = rand(); // 0 ~ 32767 or 2147483647

    // srand로 난수 초기화
    srand(time(NULL)); // time

    int r1 = rand() % 10; // 0 ~ 9
    int r2 = rand() % 100 + 1; // 1 ~ 100
    int r3 = rand() % 21 - 10; // 0 ~ 20 -> -10 ~ +10
    printf("r1 : %d, r2 : %d, r3 : %d\n", r1, r2, r3);
    */
    
}